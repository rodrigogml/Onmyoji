# Contrato Spotify

Envie `{"version":1,"operation":"me.get"}` ao wrapper. Escritas exigem `confirm: true` e perfil `read_write`. As operações são uma allowlist de catálogo, biblioteca, playlists e player.
